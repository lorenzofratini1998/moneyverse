import {UserDateFormatPipe} from './user-date-format.pipe';

describe('UserDateFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new UserDateFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
