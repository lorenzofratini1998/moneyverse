import {DecimalFormatDirective} from './decimal-format.directive';

describe('DecimalFormatDirective', () => {
  it('should create an instance', () => {
    const directive = new DecimalFormatDirective();
    expect(directive).toBeTruthy();
  });
});
