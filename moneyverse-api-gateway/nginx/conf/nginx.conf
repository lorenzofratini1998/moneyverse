events{
    worker_connections 1024;
}

http {
    server {
        location /accounts/sse {
            proxy_pass http://host.docker.internal:8092/accountsManagement/api/v1/sse;

            proxy_http_version 1.1;
            proxy_set_header Connection '';
            proxy_set_header Host $host;

            proxy_buffering off;
            proxy_cache off;
            proxy_set_header Accept-Encoding '';

            proxy_read_timeout 3600s;
            proxy_send_timeout 3600s;
        }

        location /budgets/sse {
            proxy_pass http://host.docker.internal:8093/budgetsManagement/api/v1/sse;

            proxy_http_version 1.1;
            proxy_set_header Connection '';
            proxy_set_header Host $host;

            proxy_buffering off;
            proxy_cache off;
            proxy_set_header Accept-Encoding '';

            proxy_read_timeout 3600s;
            proxy_send_timeout 3600s;
        }

        location /transactions/sse {
            proxy_pass http://host.docker.internal:8094/transactionsManagement/api/v1/sse;

            proxy_http_version 1.1;
            proxy_set_header Connection '';
            proxy_set_header Host $host;

            proxy_buffering off;
            proxy_cache off;
            proxy_set_header Accept-Encoding '';

            proxy_read_timeout 3600s;
            proxy_send_timeout 3600s;
        }
    }
}