{{- include "helm-library.secret" (list . "infisical.credentials-secret") -}}
{{- define "infisical.credentials-secret" -}}
metadata:
  name: "universal-auth-credentials"
data:
  clientId: {{ .Values.infisical.authentification.clientId | quote }}
  clientSecret: {{ .Values.infisical.authentification.clientSecret | quote }}
{{- end -}}
